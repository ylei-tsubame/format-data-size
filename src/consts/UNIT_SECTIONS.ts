import { DataSizeUnitSection } from '../types';

// Don't forget to change the unit list when changing the unit sections.
export const UNIT_SECTIONS: Readonly<DataSizeUnitSection[]> = [
  'byte',
  'ibyte',
  'bit',
  'ibit',
];
